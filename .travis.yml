sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t stefanradudragomir/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true stefanradudragomir/react-test npm test

after_success:
  - docker build -t stefanradudragomir/multi-client ./client
  - docker build -t stefanradudragomir/multi-nginx ./nginx
  - docker build -t stefanradudragomir/multi-server ./server
  - docker build -t stefanradudragomir/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub asd
  - docker push stefanradudragomir/multi-client
  - docker push stefanradudragomir/multi-nginx
  - docker push stefanradudragomir/multi-server
  - docker push stefanradudragomir/multi-worker
